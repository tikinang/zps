dummy:
  build:
    base:
      - go@1
    build:
      - go build -o zps main.go
    deploy:
      - zps
  run:
    prepare:
      - echo "prepare 3" > prepare.txt
    init:
      - echo "initializing"
    start: ./zps
