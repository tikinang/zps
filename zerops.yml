dummy:
  build:
    base:
      - go@1
    build:
      - env | grep "projectId\|serviceId"
      - go build -o zps main.go
    #    build:
    #      - export GOMODCACHE=/build/source/gomods; go build -o zps main.go
    deploy:
      - zps
  #    cache:
  #      - /gomods
  run:
    init:
      - env | grep "projectId\|serviceId"
      - echo "initializing"
      - mkdir -vp /var/www/lets/do/this
      - touch /var/www/lets/do/this/touched
    start: ./zps
